# ── KAFU CMS Admin ───────────────────────────────────────────────────────────
#
# Leave VITE_API_URL empty when running in Replit (the reverse proxy handles it).
# Set it to the full API origin when building for production deployment.
#
# Example (API on a separate subdomain):
#   VITE_API_URL=https://api.kafu.difbac.com
#
VITE_API_URL=
